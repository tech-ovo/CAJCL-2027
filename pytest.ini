[pytest]
testpaths = backend/tests
filterwarnings =
    error
    # starlette's TestClient warns that it prefers httpx2. It works correctly on
    # httpx, and this suite escalates warnings to errors, so the nudge would
    # otherwise fail every endpoint test.
    ignore::starlette.exceptions.StarletteDeprecationWarning
